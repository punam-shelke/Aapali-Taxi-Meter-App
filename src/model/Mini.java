package model;

public class Mini extends Car{

	public Mini(int baseDistance, int baseCharge, int nextDistance, int nextCharge, int additionalCharge, int limit,
			int flatCharge) {
		super(baseDistance, baseCharge, nextDistance, nextCharge, additionalCharge, limit, flatCharge);
	
	}

}
